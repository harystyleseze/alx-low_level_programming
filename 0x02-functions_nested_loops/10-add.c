#include "main.h"
/**
 * add - entry point
 *
 * @a: a is an integer
 * @b: b is an integer
 *
 * Description: adds two integers and returns the result.
 *
 * Return: Always return the result of the addition
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
